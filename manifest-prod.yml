applications:
- name: ${project_name}-prod
  routes:
    - route: ${project_name}-prod.apps.use.sandbox.pcf.manulife.com
  buildpacks:
    - java_buildpack_offline
  memory: 1024M
  disk_quota: 256M

